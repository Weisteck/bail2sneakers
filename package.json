{
  "name": "yarn-monorepo",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "workspaces": [
	"frontend",
	"backend",
	"packages/*"
  ],
  "scripts": {
	"start-back": "yarn --cwd backend start",
	"start-front": "yarn --cwd frontend dev",
	"build": "yarn --cwd frontend build"
  }
}
